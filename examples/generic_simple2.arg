// Generic struct with method
struct Wrapper<T> {
    value: T;

    get(): T with &this {
        return this.value;
    }
}

const intBox = Wrapper { value: 10 };
const strBox = Wrapper { value: "hello" };

console.log(intBox.get());
console.log(strBox.get());
