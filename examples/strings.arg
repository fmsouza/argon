// String operations and template interpolation

from "std:io" import { println };

const greeting = "Hello";
const name = "Argon";
const message = greeting + ", " + name;
const templated = `${greeting} ${name} v${1}`;

println(message);
println(templated);
