// Generic struct + alias + object literal assignment

from "std:io" import { println };

struct Container<T> {
    value: T;
}

type NumberContainer = Container<i32>;
const container: NumberContainer = { value: 42 };
println(container.value);
