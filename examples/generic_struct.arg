// Generic struct with method
struct Pair<T> {
    left: T;
    right: T;

    first(): T with &this {
        return this.left;
    }
}

const ints = Pair { left: 1, right: 2 };
println(ints.first());
