// Object literals in expression position
struct Config {
    host: string;
    port: i32;
}

const config: Config = { host: "localhost", port: 8080 };
const payload = { value: 42, nested: { ok: true } };

println(config.host);
println(payload.value);
