// Struct method with `with &this`
struct Point {
    x: f64;
    y: f64;

    lengthSquared(): f64 with &this {
        return this.x * this.x + this.y * this.y;
    }
}

const p = Point { x: 3.0, y: 4.0 };
console.log(p.lengthSquared());
