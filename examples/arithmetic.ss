// Basic arithmetic with typed integers
const a: i32 = 10;
const b: i32 = 5;

const sum: i32 = a + b;
const diff: i32 = a - b;
const product: i32 = a * b;
const quotient: f64 = a / b;

console.log(sum);
console.log(diff);
console.log(product);
console.log(quotient);
