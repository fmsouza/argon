// Argon module syntax
from "reflect-metadata" import;
from "axios" import Axios;
from "react" import { useState };

export const apiBase: string = "/api";

export function readEnv(): string {
    return apiBase;
}

const stateFactory = useState;
const client = Axios;
println(stateFactory);
println(client);
