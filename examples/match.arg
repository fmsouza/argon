// Match statement

from "std:io" import { println };

function classify(n: i32): i32 {
    match (n) {
        0 => return 1,
        1 => return 2,
        2 => return 3,
    }
    return 0;
}

println(classify(0));
println(classify(1));
println(classify(9));
