// Boolean logic and conditionals

from "std:io" import { println };

const a = true;
const b = false;
const both = a && b;
const either = a || b;
const inverted = !b;

if (both) {
    println(0);
} else {
    println(1);
}

println(either);
println(inverted);
