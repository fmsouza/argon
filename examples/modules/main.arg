// Multi-module example: importing from other argon files
from "./math" import { add, multiply, PI };
from "./utils" import Utils;

const sum = add(2, 3);
const product = multiply(4, 5);
println(Utils.greet("Argon"));
println("Version: " + Utils.VERSION);
println(sum);
println(product);
println(PI);
