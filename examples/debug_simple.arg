// try/catch/finally
function guardedDivide(a: i32, b: i32): i32 {
    try {
        if (b == 0) {
            throw 1;
        }
        return a / b;
    } catch (err) {
        const captured = err;
        console.log(captured);
        return 0;
    } finally {
        const cleanupDone = true;
        console.log(cleanupDone);
    }

    return 0;
}

console.log(guardedDivide(10, 2));
console.log(guardedDivide(10, 0));
